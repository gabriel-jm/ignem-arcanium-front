import { IgnemElement } from '@/ui/view/ignem-element'
import { html } from 'lithen-tag-functions'

export class IgnemCharactersShortList extends IgnemElement {
  render() {
    return html`
      <div>
        
      </div>
    `
  }
}
